# Toast 提示 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 应用调用一次 `mc_toast(ctx, "文本", duration_ms, pos)` 即可在最上层弹出一条提示，固定时间后自动消失，不占焦点、不可触摸。

**Architecture:** 应用侧 `mc_toast()` 只把文本打包成一条 `ui/toast` bus 消息（不创建 surface）。一个常驻的 **toast 守护进程**订阅该 topic，收到后用已跑通的 LVGL 渲染一个半透明卡片到一个 `TOAST` 角色的 mc surface（z=200，压在所有全屏/popup 之上），按 `pos` 摆放，计时到点销毁。合成器对 `TOAST` 角色在命中测试中跳过 → 不可触摸、不抢焦点。并发为**替换语义**：新 Toast 先销毁旧的再显示（每次显隐都是一次全量重合成，保证叠加正确）。

**Tech Stack:** C99；mc 自有 bus/surface 协议；LVGL 9.0（`ports/lvgl/lv_port_mc.c`）；交叉编译 T113/T507（`build.sh`）。

**关键事实（已核对源码）：**
- 角色 z：`compositor/surface.c:30 default_z_order()`（static），POPUP=100/BG=0/FULLSCREEN=10。
- `compositor/transport.c:326` **重复**了一份 z 计算 `(role==2)?100:(role==3?0:10)`，`SET_ROLE` 时会重算 z（`mc_surface_set_popup_pos()` 走的就是 `SET_ROLE`）—— 两处都要认识 role 4，本计划用导出 `default_z_order` 消除重复。
- 命中测试：`compositor/input.c:11 mc_input_hit_test()` 按 z 降序取第一个命中的 surface。
- 合成：`compositor/compose.c:312` role==1 走不透明 blit，其余（含 role 4）走 else 的 alpha 混合；遮挡剔除只把 role==1 当遮挡物（`compose.c:231`），role 4 不会挡住下层。**无需改 compose.c**。
- 服务端不校验 role 上界（`surface.c:86 sf->role = role ? role : 1;`），role 4 直接可用。
- LVGL 端口是单实例全局态（`g_disp/g_surf`），DIRECT 已改 FULL 模式；**缺 deinit**，本计划补一个 `lv_port_mc_deinit()` 以便守护进程每条 Toast 重建/销毁 display 不泄漏。
- bus payload 上限 4KB；发布者收不到自己的消息（`libmc/include/mc.h:188`）。
- demo-popup（`examples/demo-popup/main.c`）是守护进程的最佳模板。

---

## File Structure

- **Create** `common/mc_toast_wire.h` — `ui/toast` 线协议的编码/解码（`static inline`，libmc 与守护进程与单元测试共用）。
- **Modify** `libmc/include/mc.h` — 新增 `MC_ROLE_TOAST`、`mc_toast_pos_t`、`mc_toast()` 原型。
- **Create** `libmc/src/toast.c` — `mc_toast()` 实现（打包 + `mc_bus_publish`）。
- **Create** `tools/mc-toast-wire-test.c` — `mc_toast_wire.h` 的宿主机单元测试（纯函数，TDD）。
- **Modify** `compositor/surface.c` / `compositor/surface.h` — 导出 `default_z_order` 并加 role 4 → z=200。
- **Modify** `compositor/transport.c` — `handle_set_role` 改用 `default_z_order`（消除重复，认识 role 4）。
- **Modify** `compositor/input.c` — `mc_input_hit_test` 跳过 `role==MC_ROLE_TOAST`。
- **Modify** `ports/lvgl/lv_port_mc.c` / `lv_port_mc.h` — 新增 `lv_port_mc_deinit()`。
- **Create** `examples/toast-daemon/main.c` — 守护进程。
- **Modify** `Makefile` — libmc 加 `toast.c`；新增 `mc-toast-wire-test`、`toast-daemon` 目标。
- **Modify** `build.sh` — 打包 `toast-daemon`，`start.sh` 拉起它。
- **Modify** `docs/...` / 收尾。

每个 Task 都是独立可编译、可提交的最小改动。Task 1（线协议+API）和 Task 2/3（合成器）互相独立；Task 4（端口 deinit）独立；Task 5（守护进程）依赖 1/4；Task 6 串起构建；Task 7 真机验证。

---

## Task 1: 线协议 codec + `mc_toast()` API（含宿主机单元测试）

**Files:**
- Create: `common/mc_toast_wire.h`
- Create: `tools/mc-toast-wire-test.c`
- Modify: `libmc/include/mc.h`（在 `mc_role_t` 和 bus 段之间插入）
- Create: `libmc/src/toast.c`
- Modify: `Makefile`（`LIBMC_SRC` 加 `toast.c`；新增 `mc-toast-wire-test` 目标）

- [ ] **Step 1: 写线协议头（先写实现，因为下一步的测试要 include 它）**

Create `common/mc_toast_wire.h`：

```c
/*
 * Wire format for the "ui/toast" bus message. Little-endian, hand-packed
 * (no struct-padding reliance). Shared by libmc (mc_toast), the toast
 * daemon (decode), and the unit test.
 *
 * Layout:  [0..1] magic 'TS' LE | [2] version | [3] pos
 *          [4..7] duration_ms LE | [8..] UTF-8 text (no NUL, len = total-8)
 */
#ifndef MC_TOAST_WIRE_H
#define MC_TOAST_WIRE_H

#include <stdint.h>
#include <string.h>

#define MC_TOAST_MAGIC       0x5453u   /* 'T','S' little-endian */
#define MC_TOAST_VERSION     1
#define MC_TOAST_HDR_BYTES   8
#define MC_TOAST_MAX_TEXT    512        /* well under the 4KB bus cap */
#define MC_TOAST_DEFAULT_MS  2000

/* pos values mirror mc_toast_pos_t (0=bottom,1=center,2=top). */

/* Back off `n` so it never splits a UTF-8 multibyte sequence: if byte n
 * is a continuation byte (10xxxxxx), walk back to the lead byte. Returns a
 * safe length <= n. */
static inline size_t mc_toast_utf8_floor(const char *s, size_t n)
{
    while (n > 0 && ((unsigned char)s[n] & 0xC0) == 0x80) n--;
    return n;
}

/* Encode into `out` (cap >= MC_TOAST_HDR_BYTES + MC_TOAST_MAX_TEXT).
 * Returns total byte count, or -1 on bad args. duration<=0 and out-of-range
 * pos are normalized by the CALLER (mc_toast); here we just pack. */
static inline int mc_toast_wire_encode(uint8_t *out, size_t outcap,
                                       const char *text,
                                       uint32_t duration_ms, uint8_t pos)
{
    if (!out || !text) return -1;
    if (outcap < MC_TOAST_HDR_BYTES) return -1;
    size_t tlen = strlen(text);
    size_t maxt = outcap - MC_TOAST_HDR_BYTES;
    if (maxt > MC_TOAST_MAX_TEXT) maxt = MC_TOAST_MAX_TEXT;
    if (tlen > maxt) tlen = mc_toast_utf8_floor(text, maxt);
    out[0] = MC_TOAST_MAGIC & 0xff;
    out[1] = (MC_TOAST_MAGIC >> 8) & 0xff;
    out[2] = MC_TOAST_VERSION;
    out[3] = pos;
    out[4] =  duration_ms        & 0xff;
    out[5] = (duration_ms >> 8)  & 0xff;
    out[6] = (duration_ms >> 16) & 0xff;
    out[7] = (duration_ms >> 24) & 0xff;
    memcpy(out + MC_TOAST_HDR_BYTES, text, tlen);
    return (int)(MC_TOAST_HDR_BYTES + tlen);
}

/* Decode a received payload. `text_out` gets a NUL-terminated copy
 * (cap text_cap). Returns 0 on success, -1 on malformed/short/bad-magic. */
static inline int mc_toast_wire_decode(const void *payload, size_t len,
                                       uint32_t *duration_ms, uint8_t *pos,
                                       char *text_out, size_t text_cap)
{
    const uint8_t *p = (const uint8_t *)payload;
    if (!p || len < MC_TOAST_HDR_BYTES) return -1;
    uint16_t magic = (uint16_t)p[0] | ((uint16_t)p[1] << 8);
    if (magic != MC_TOAST_MAGIC) return -1;
    if (p[2] != MC_TOAST_VERSION) return -1;
    if (pos) *pos = p[3];
    if (duration_ms)
        *duration_ms = (uint32_t)p[4] | ((uint32_t)p[5] << 8)
                     | ((uint32_t)p[6] << 16) | ((uint32_t)p[7] << 24);
    size_t tlen = len - MC_TOAST_HDR_BYTES;
    if (text_out && text_cap) {
        if (tlen > text_cap - 1) tlen = mc_toast_utf8_floor((const char *)(p + 8), text_cap - 1);
        memcpy(text_out, p + MC_TOAST_HDR_BYTES, tlen);
        text_out[tlen] = '\0';
    }
    return 0;
}

#endif /* MC_TOAST_WIRE_H */
```

- [ ] **Step 2: 写失败的单元测试**

Create `tools/mc-toast-wire-test.c`：

```c
/* Host unit test for common/mc_toast_wire.h. Build & run:
 *   cc -Icommon -o build/mc-toast-wire-test tools/mc-toast-wire-test.c && build/mc-toast-wire-test
 */
#include "mc_toast_wire.h"
#include <assert.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    uint8_t buf[MC_TOAST_HDR_BYTES + MC_TOAST_MAX_TEXT];

    /* round-trip ASCII */
    int n = mc_toast_wire_encode(buf, sizeof(buf), "hello", 1500, 2);
    assert(n == MC_TOAST_HDR_BYTES + 5);
    uint32_t dur = 0; uint8_t pos = 0; char txt[64] = {0};
    assert(mc_toast_wire_decode(buf, (size_t)n, &dur, &pos, txt, sizeof(txt)) == 0);
    assert(dur == 1500 && pos == 2 && strcmp(txt, "hello") == 0);

    /* bad magic rejected */
    buf[0] = 0; assert(mc_toast_wire_decode(buf, (size_t)n, &dur, &pos, txt, sizeof(txt)) == -1);

    /* short payload rejected */
    assert(mc_toast_wire_decode(buf, 4, &dur, &pos, txt, sizeof(txt)) == -1);

    /* UTF-8 truncation never splits a codepoint: "中" = E4 B8 AD (3 bytes).
     * Encode into a buffer whose text room is 2 -> must drop the whole char. */
    uint8_t small[MC_TOAST_HDR_BYTES + 2];
    int m = mc_toast_wire_encode(small, sizeof(small), "\xE4\xB8\xAD", 1000, 0);
    assert(m == MC_TOAST_HDR_BYTES + 0);   /* 3-byte char dropped, not split */

    printf("mc-toast-wire-test: OK\n");
    return 0;
}
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cc -Icommon -o build/mc-toast-wire-test tools/mc-toast-wire-test.c 2>&1 | head`
Expected: 编译失败 `fatal error: mc_toast_wire.h: No such file or directory` —— 若 Step 1 已建文件则改为：先临时把 `common/mc_toast_wire.h` 重命名验证测试确实依赖它，再恢复。（实际执行中 Step 1 已建好头文件，本步主要确认测试能编译并运行；若想看红，先 `git stash -- common/mc_toast_wire.h`。）

- [ ] **Step 4: 跑测试确认通过**

Run: `cc -Icommon -o build/mc-toast-wire-test tools/mc-toast-wire-test.c && ./build/mc-toast-wire-test`
Expected: `mc-toast-wire-test: OK`

- [ ] **Step 5: 加 public API 到 `libmc/include/mc.h`**

在 `mc_role_t` 枚举（结尾 `} mc_role_t;`，约 mc.h:28）之后插入：

```c
typedef enum {
    MC_ROLE_FULLSCREEN = 1,
    MC_ROLE_POPUP      = 2,
    MC_ROLE_BG         = 3,
    MC_ROLE_TOAST      = 4,   /* 最上层、不可触摸的提示层（compositor z=200） */
} mc_role_t;
```

并在 bus 段之前（约 mc.h:179 `/* Bus ... */` 之上）加：

```c
/* ---- toast ----
 * 弹出一条最上层、不占焦点、不可触摸、定时消失的文字提示。
 * 本质是一次 bus publish（topic "ui/toast"），由 toast 守护进程渲染。
 *   text:        UTF-8；过长按 UTF-8 边界截断。
 *   duration_ms: <=0 用默认 2000ms。
 *   pos:         位置，越界回落 BOTTOM。
 * 返回 0 成功，负值为 MC_E_*（守护进程未运行时仍返回 0，只是看不到提示）。*/
typedef enum {
    MC_TOAST_POS_BOTTOM = 0,
    MC_TOAST_POS_CENTER = 1,
    MC_TOAST_POS_TOP    = 2,
} mc_toast_pos_t;

int mc_toast(mc_ctx_t *ctx, const char *text, int duration_ms, mc_toast_pos_t pos);
```

（注意：把现有的 `MC_ROLE_*` 枚举整体替换成上面带 `MC_ROLE_TOAST` 的版本，不要重复定义。）

- [ ] **Step 6: 实现 `mc_toast()`**

Create `libmc/src/toast.c`：

```c
#include "mc.h"
#include "mc_toast_wire.h"
#include "internal.h"   /* MC_E_* */

int mc_toast(mc_ctx_t *ctx, const char *text, int duration_ms, mc_toast_pos_t pos)
{
    if (!ctx || !text) return MC_E_INVAL;
    uint32_t dur = (duration_ms <= 0) ? MC_TOAST_DEFAULT_MS : (uint32_t)duration_ms;
    uint8_t p = (pos < MC_TOAST_POS_BOTTOM || pos > MC_TOAST_POS_TOP)
              ? MC_TOAST_POS_BOTTOM : (uint8_t)pos;

    uint8_t buf[MC_TOAST_HDR_BYTES + MC_TOAST_MAX_TEXT];
    int n = mc_toast_wire_encode(buf, sizeof(buf), text, dur, p);
    if (n < 0) return MC_E_INVAL;
    return mc_bus_publish(ctx, "ui/toast", buf, (uint32_t)n);
}
```

确认 `libmc/src/internal.h` 提供了 `MC_E_INVAL`（连接/surface 文件都在用，应已包含；若不在 internal.h 则它在 `mc.h` 的错误码里——`mc.h` 已被 include）。

- [ ] **Step 7: 接进 Makefile（libmc 源 + 测试目标 + 头 include 路径）**

`Makefile:158` 的 `LIBMC_SRC` 追加一行（在 `util.c` 那行后）：

```make
LIBMC_SRC   := libmc/src/connect.c \
               libmc/src/surface.c \
               libmc/src/bus.c \
               libmc/src/toast.c \
               libmc/src/util.c
```

`toast.c` 需要 `-Icommon`（线协议头）。确认 `CFLAGS` 已含 `-Icommon`（`Makefile:133` 已有 `-Icommon -Ilibmc/include`）—— 已满足，无需改 CFLAGS。

在 `demos:` 目标（`Makefile:212`）下方加一个宿主机测试目标，并加入 `all`：

```make
# 纯函数线协议单元测试（宿主机 cc 即可，不依赖交叉/LVGL）
WIRE_TEST_BIN := $(OUT)/mc-toast-wire-test
$(WIRE_TEST_BIN): tools/mc-toast-wire-test.c common/mc_toast_wire.h
	@mkdir -p $(OUT)
	$(CC) $(CFLAGS_ALL) -o $@ tools/mc-toast-wire-test.c

wire-test: $(WIRE_TEST_BIN)
	$(WIRE_TEST_BIN)
```

并在 `.PHONY`（`Makefile:267`）追加 `wire-test`。

- [ ] **Step 8: 构建并跑测试**

Run: `make wire-test`
Expected: 编译通过并打印 `mc-toast-wire-test: OK`

Run: `make`（确认 libmc 仍能链接，`toast.c` 进了 `libmc.a`）
Expected: 无错误。

- [ ] **Step 9: 提交**

```bash
git add common/mc_toast_wire.h tools/mc-toast-wire-test.c libmc/include/mc.h libmc/src/toast.c Makefile
git commit -m "libmc: mc_toast() API + ui/toast wire codec + host unit test"
```

---

## Task 2: 合成器 TOAST 角色（z=200，消除 z 计算重复）

**Files:**
- Modify: `compositor/surface.c:30-38`（`default_z_order`：去 static + 加 case 4）
- Modify: `compositor/surface.h`（声明 `default_z_order`）
- Modify: `compositor/transport.c:326`（改调用 `default_z_order`）

- [ ] **Step 1: 导出并扩展 `default_z_order`**

`compositor/surface.c:30` 把：

```c
static int default_z_order(uint8_t role)
{
    switch (role) {
    case 2: return 100;   /* POPUP */
    case 3: return 0;     /* BG */
    case 1: default:
        return 10;        /* FULLSCREEN */
    }
}
```

改为（去掉 `static`，加 role 4）：

```c
int default_z_order(uint8_t role)
{
    switch (role) {
    case 4: return 200;   /* TOAST — above everything */
    case 2: return 100;   /* POPUP */
    case 3: return 0;     /* BG */
    case 1: default:
        return 10;        /* FULLSCREEN */
    }
}
```

- [ ] **Step 2: 在 `surface.h` 声明它**

`compositor/surface.h` 现有 `mc_surface_create` 等声明附近（如 `mc_surface_get` 声明之后）加：

```c
/* Default z-order for a role (FULLSCREEN=10, POPUP=100, BG=0, TOAST=200).
 * Shared by surface creation and SET_ROLE handling so the two never drift. */
int default_z_order(uint8_t role);
```

- [ ] **Step 3: `handle_set_role` 改用它（消除重复 + 认识 role 4）**

`compositor/transport.c:326` 把：

```c
    sf->z_order = (sf->role == 2) ? 100 : (sf->role == 3 ? 0 : 10);
```

改为：

```c
    sf->z_order = default_z_order(sf->role);
```

`transport.c` 已 `#include "surface.h"`（它调用了 `mc_surface_get` 等），无需新增 include；若编译报未声明，确认 include 存在。

- [ ] **Step 4: 构建合成器**

Run: `make $(: 本机native) ` → 实际命令：`make build/mc-compositor` 或直接 `make`
Expected: 编译通过，无 “default_z_order” 相关 warning/error。

- [ ] **Step 5: 提交**

```bash
git add compositor/surface.c compositor/surface.h compositor/transport.c
git commit -m "compositor: MC_ROLE_TOAST z=200; share default_z_order (kill dup z calc)"
```

---

## Task 3: TOAST 层输入透明（不可触摸 / 不抢焦点）

**Files:**
- Modify: `compositor/input.c:31-36`（`mc_input_hit_test` 命中循环）

- [ ] **Step 1: 命中测试跳过 TOAST**

`compositor/input.c` 命中循环（当前 31-36 行）：

```c
    for (int i = 0; i < n; i++) {
        struct mc_surface *sf = list[i];
        int sx0 = sf->x, sy0 = sf->y;
        int sx1 = sx0 + sf->w, sy1 = sy0 + sf->h;
        if (x >= sx0 && x < sx1 && y >= sy0 && y < sy1) return sf;
    }
```

改为（顶部加跳过 role 4；用字面量 4 以免 input.c 依赖 libmc 的 mc.h，保持与该文件现有 `role != 1` 风格一致，并加注释）：

```c
    for (int i = 0; i < n; i++) {
        struct mc_surface *sf = list[i];
        /* TOAST layer (role 4) is input-transparent: never grabs touch,
         * so it can't steal focus and taps fall through to the app below. */
        if (sf->role == 4 /* MC_ROLE_TOAST */) continue;
        int sx0 = sf->x, sy0 = sf->y;
        int sx1 = sx0 + sf->w, sy1 = sy0 + sf->h;
        if (x >= sx0 && x < sx1 && y >= sy0 && y < sy1) return sf;
    }
```

- [ ] **Step 2: 构建**

Run: `make`
Expected: 编译通过。

- [ ] **Step 3: 提交**

```bash
git add compositor/input.c
git commit -m "compositor: hit-test skips TOAST role (untouchable, never focuses)"
```

---

## Task 4: `lv_port_mc_deinit()`（端口可重复 init/deinit）

**Files:**
- Modify: `ports/lvgl/lv_port_mc.h`（声明）
- Modify: `ports/lvgl/lv_port_mc.c`（实现）

守护进程每条 Toast 都 `init`（建 display）→ 显示 → `deinit`（删 display），避免长跑泄漏。

- [ ] **Step 1: 声明 deinit**

`ports/lvgl/lv_port_mc.h` 在 `lv_port_mc_init(...)` 声明（第 15 行）之后加：

```c
/* Tear down the display created by lv_port_mc_init so the port can be
 * re-init'd against a different surface (e.g. a toast shown repeatedly).
 * Deletes the LVGL display (and its screen objects) and drops the surface
 * reference. Safe to call when not initialized. */
void lv_port_mc_deinit(void);
```

- [ ] **Step 2: 实现 deinit**

`ports/lvgl/lv_port_mc.c` 在 `lv_port_mc_init(...)` 函数（结尾 `return 0; }`，约第 271 行）之后追加：

```c
void lv_port_mc_deinit(void)
{
    if (g_disp) {
        lv_display_delete(g_disp);   /* also frees its default screen */
        g_disp = NULL;
    }
    g_surf = NULL;
    g_buf_addr[0] = g_buf_addr[1] = NULL;
    g_dmg_n = 0;
    g_n_holes = 0;
    g_last_committed_idx = -1;
}
```

- [ ] **Step 3: 构建端口（随 demo 一起编译）**

Run: `make CROSS=1 demos LVGL_PLATFORM=T507`（或宿主无 LVGL 时跳过，留待 Task 6 整体交叉构建验证）
Expected: 端口 `.o` 编译通过；`lv_display_delete` 是 LVGL 9 标准 API。

- [ ] **Step 4: 提交**

```bash
git add ports/lvgl/lv_port_mc.h ports/lvgl/lv_port_mc.c
git commit -m "lv_port_mc: add lv_port_mc_deinit (re-init support for toast daemon)"
```

---

## Task 5: toast 守护进程

**Files:**
- Create: `examples/toast-daemon/main.c`

行为：连接 mc → 订阅 `ui/toast` → 主循环 `poll(mc_fd, 剩余时长)` + `mc_dispatch` 收 bus → `show()`/`dismiss()`。
**替换语义 = 每次 show 先 dismiss 旧的再建新的**，因此每次显隐都触发合成器全量重合成（叠加正确，无残影）。
渲染：全屏宽透明 surface（screen_w × BAND_H），LVGL 画一个 ≤80% 屏宽、自动换行的半透明卡片，水平居中；`pos` 决定这条 band 在屏幕上的纵向位置（set_popup_pos）。

- [ ] **Step 1: 写守护进程**

Create `examples/toast-daemon/main.c`：

```c
/*
 * toast-daemon: renders "ui/toast" bus messages as a top-most, untouchable,
 * auto-dismissing text card.
 *
 * - subscribes to "ui/toast" (payload = common/mc_toast_wire.h)
 * - on each toast: (re)creates a TOAST-role mc surface (screen_w x BAND_H),
 *   draws a centered translucent card+label with LVGL, positions the band by
 *   gravity, renders one frame, arms a timer.
 * - replace semantics: a new toast first dismisses the current one.
 * - the surface is destroyed on dismiss, so there is ZERO compose cost while
 *   no toast is showing.
 */
#define _GNU_SOURCE
#include "mc.h"
#include "mc_toast_wire.h"
#include "lv_port_mc.h"

#include <poll.h>
#include <signal.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

#define BAND_H        160     /* surface height; room for ~3 wrapped lines */
#define MARGIN        24      /* top/bottom gap from screen edge */
#define CARD_MAX_FRAC 80      /* card max width = 80% of screen width */

static volatile sig_atomic_t s_running = 1;
static void on_sig(int sig) { (void)sig; s_running = 0; }

static mc_ctx_t      *g_ctx;
static int            g_scr_w, g_scr_h;

/* Current toast state */
static mc_surface_t  *g_surf;       /* NULL when nothing showing */
static int            g_visible;
static uint64_t       g_expire_ms;  /* monotonic ms when current toast ends */

static uint64_t now_ms(void)
{
    struct timespec t;
    clock_gettime(CLOCK_MONOTONIC, &t);
    return (uint64_t)t.tv_sec * 1000 + (uint64_t)t.tv_nsec / 1000000;
}

/* y of the band for a given gravity (pos). */
static int band_y_for(uint8_t pos)
{
    switch (pos) {
    case MC_TOAST_POS_TOP:    return MARGIN;
    case MC_TOAST_POS_CENTER: return (g_scr_h - BAND_H) / 2;
    case MC_TOAST_POS_BOTTOM:
    default:                  return g_scr_h - BAND_H - MARGIN;
    }
}

static void build_card(const char *text)
{
    lv_obj_t *scr = lv_screen_active();
    lv_obj_set_style_bg_opa(scr, LV_OPA_TRANSP, LV_PART_MAIN);  /* band is transparent */

    lv_obj_t *card = lv_obj_create(scr);
    lv_obj_set_width(card, LV_SIZE_CONTENT);
    lv_obj_set_height(card, LV_SIZE_CONTENT);
    lv_obj_set_style_max_width(card, (g_scr_w * CARD_MAX_FRAC) / 100, LV_PART_MAIN);
    lv_obj_align(card, LV_ALIGN_CENTER, 0, 0);                  /* centered in band */
    lv_obj_set_style_bg_color(card, lv_color_hex(0x000000), LV_PART_MAIN);
    lv_obj_set_style_bg_opa  (card, LV_OPA_80,             LV_PART_MAIN);
    lv_obj_set_style_radius  (card, 12,                    LV_PART_MAIN);
    lv_obj_set_style_border_width(card, 0,                 LV_PART_MAIN);
    lv_obj_set_style_pad_hor (card, 18, LV_PART_MAIN);
    lv_obj_set_style_pad_ver (card, 12, LV_PART_MAIN);

    lv_obj_t *lbl = lv_label_create(card);
    lv_label_set_long_mode(lbl, LV_LABEL_LONG_WRAP);
    lv_obj_set_width(lbl, (g_scr_w * CARD_MAX_FRAC) / 100 - 36); /* minus h-pad */
    lv_label_set_text(lbl, text);
    lv_obj_set_style_text_color(lbl, lv_color_hex(0xFFFFFF), LV_PART_MAIN);
}

static void dismiss(void)
{
    if (!g_visible) return;
    lv_port_mc_deinit();
    if (g_surf) { mc_surface_destroy(g_surf); g_surf = NULL; }
    g_visible = 0;
}

static void show(const char *text, uint32_t dur_ms, uint8_t pos)
{
    dismiss();   /* replace semantics: drop any current toast first */

    g_surf = mc_surface_create_shm(g_ctx, g_scr_w, BAND_H,
                                   MC_FMT_BGRA8888, MC_ROLE_TOAST, 2);
    if (!g_surf) {
        fprintf(stderr, "[toast] create surface failed: %s\n",
                mc_strerror(mc_last_error()));
        return;
    }
    mc_surface_set_popup_pos(g_surf, 0, band_y_for(pos));

    if (lv_port_mc_init(g_surf, g_scr_w, BAND_H, 0) < 0) {
        fprintf(stderr, "[toast] lv_port_mc_init failed\n");
        mc_surface_destroy(g_surf); g_surf = NULL;
        return;
    }
    build_card(text);
    lv_refr_now(NULL);                 /* render+commit one frame immediately */

    g_visible   = 1;
    g_expire_ms = now_ms() + dur_ms;
    fprintf(stderr, "[toast] show pos=%u %ums: \"%s\"\n", pos, dur_ms, text);
}

static void handle_bus(const mc_event_t *ev)
{
    if (ev->kind != MC_EV_BUS) return;
    if (strcmp(ev->bus.topic, "ui/toast") != 0) return;
    uint32_t dur = MC_TOAST_DEFAULT_MS; uint8_t pos = MC_TOAST_POS_BOTTOM;
    char text[MC_TOAST_MAX_TEXT + 1];
    if (mc_toast_wire_decode(ev->bus.data, ev->bus.len, &dur, &pos,
                             text, sizeof(text)) != 0) {
        fprintf(stderr, "[toast] malformed ui/toast payload (len=%u)\n", ev->bus.len);
        return;
    }
    show(text, dur, pos);
}

int main(int argc, char **argv)
{
    const char *sock = NULL;
    for (int i = 1; i < argc; i++) {
        if (!strcmp(argv[i], "--socket") && i + 1 < argc) sock = argv[++i];
    }
    if (sock) mc_set_socket_path(sock);

    signal(SIGINT,  on_sig);
    signal(SIGTERM, on_sig);

    g_ctx = mc_connect("toast-daemon");
    if (!g_ctx) {
        fprintf(stderr, "[toast] connect failed: %s\n", mc_strerror(mc_last_error()));
        return 1;
    }
    mc_screen_info_t si;
    if (mc_get_screen_info(g_ctx, &si) != 0) {
        fprintf(stderr, "[toast] get_screen_info failed\n");
        return 1;
    }
    g_scr_w = si.screen_w; g_scr_h = si.screen_h;
    fprintf(stderr, "[toast] connected, screen %dx%d\n", g_scr_w, g_scr_h);

    lv_init();
    mc_bus_subscribe(g_ctx, "ui/toast");

    int fd = mc_fd(g_ctx);
    while (s_running) {
        if (!mc_alive(g_ctx)) { fprintf(stderr, "[toast] compositor gone\n"); break; }

        int timeout = -1;  /* block until a bus message arrives */
        if (g_visible) {
            uint64_t now = now_ms();
            timeout = (now >= g_expire_ms) ? 0 : (int)(g_expire_ms - now);
        }
        struct pollfd p = { .fd = fd, .events = POLLIN };
        poll(&p, 1, timeout);

        /* Drain all pending mc messages (bus etc.). */
        mc_event_t ev;
        while (mc_dispatch(g_ctx, &ev, 0) > 0) handle_bus(&ev);

        if (g_visible && now_ms() >= g_expire_ms) {
            fprintf(stderr, "[toast] dismiss\n");
            dismiss();
        }
    }

    dismiss();
    mc_disconnect(g_ctx);
    return 0;
}
```

> 实现注记给执行者：
> - 不调用 `lv_port_mc_input_init()`——守护进程不需要触摸，bus 消息由本进程 `mc_dispatch` 直接收。
> - `lv_refr_now(NULL)` 刷新默认 display（本进程只有一个 display）；它会触发 `mc_flush_cb` → `mc_surface_commit_idx`，首帧 commit 让合成器把 TOAST 层合成上屏。
> - 若 `LV_SIZE_CONTENT`/`lv_obj_set_style_max_width`/`LV_LABEL_LONG_WRAP` 在本仓库的 `lv_conf.h` 下某个不可用，退化方案：卡片用固定宽度 `g_scr_w*0.8`、label 用 `LV_LABEL_LONG_WRAP`。执行时以 `make CROSS=1` 的编译结果为准（见 Task 6 Step 2）。

- [ ] **Step 2: 等待 Task 6 接好构建后再编译**（本任务仅产出源文件）

本步无独立构建（守护进程要 LVGL 链接，构建规则在 Task 6 加）。先提交源码。

- [ ] **Step 3: 提交**

```bash
git add examples/toast-daemon/main.c
git commit -m "examples: toast-daemon (renders ui/toast as top-most untouchable card)"
```

---

## Task 6: 构建接线（Makefile / build.sh / start.sh）

**Files:**
- Modify: `Makefile`（toast-daemon 目标，仿 demo-popup）
- Modify: `build.sh`（打包 + start.sh 拉起）

- [ ] **Step 1: Makefile 加 toast-daemon 目标**

`Makefile` 在 demo-popup 变量块（200-202）之后加：

```make
TOAST_SRC   := examples/toast-daemon/main.c
TOAST_OBJ   := $(TOAST_SRC:%.c=$(OUT)/%.o)
TOAST_BIN   := $(OUT)/toast-daemon
```

在 LVGL flags 块（205-207）加一行让它带上 LVGL include（toast-daemon 还需要 `-Icommon`，已在全局 CFLAGS）：

```make
$(TOAST_OBJ): MC_DEMO_CFLAGS=$(LVGL_INC)
```

`demos:` 目标（212）改为也产出 toast-daemon：

```make
demos: $(FS_BIN) $(POP_BIN) $(TOAST_BIN)
```

在 demo-popup 链接规则（247-248）之后加链接规则：

```make
$(TOAST_BIN): $(TOAST_OBJ) $(PORT_OBJ) $(LIBMC_A) $(LVGL_LIB)
	$(CC) $(CFLAGS_ALL) -o $@ $(TOAST_OBJ) $(PORT_OBJ) $(LIBMC_A) $(LVGL_LIBS) $(LDFLAGS_ALL)
```

- [ ] **Step 2: 交叉构建验证（T113）**

Run（按仓库现有方式，参考 build.sh）：`make CROSS=1 demos LVGL_PLATFORM=T113`
Expected: 产出 `build/toast-daemon`，无编译/链接错误。若 LVGL 样式 API 报错，按 Task 5 Step1 的退化方案调整 `build_card` 后重编。

- [ ] **Step 3: build.sh 打包 toast-daemon**

`build.sh:177`（`cp -v build/demo-popup "$STAGE/bin/"`）之后加：

```sh
    cp -v build/toast-daemon    "$STAGE/bin/"
```

`build.sh:372` 与 `:429` 的 `ps | grep -E "..."` 进程清单各加上 `toast-daemon`（用于 stop/清理）：

```
mc-compositor|demo-fullscreen|demo-popup|awtk-demo|toast-daemon
```

- [ ] **Step 4: start.sh 拉起 toast-daemon**

`build.sh` 生成 start.sh 的 here-doc 里，在 demo-popup 的 `launch` 块（约 :415-418）之后加：

```sh
if [ -x "\$HERE/bin/toast-daemon" ]; then
    launch toast-daemon \\
        "\$HERE/bin/toast-daemon --socket /tmp/mc.sock"
fi
```

（紧贴现有 `launch` 用法与转义风格；`toast-daemon` 不带位置参数，位置由应用每条 toast 指定。）

- [ ] **Step 5: 提交**

```bash
git add Makefile build.sh
git commit -m "build: build/package/launch toast-daemon (T113/T507)"
```

---

## Task 7: 真机集成验证 + 文档

**Files:**
- Modify: `examples/demo-popup/main.c`（在 OK 回调里加一行 `mc_toast` 作为真机触发器，便于验证）
- Modify: `README.md` 或 `docs/`（简述 Toast 用法）

本项目无单测框架，集成靠真机（与既有 `t113-awtk-working` 流程一致）。

- [ ] **Step 1: 给 demo-popup 的 OK 回调加一个真机触发器**

`examples/demo-popup/main.c` 的 `on_ok_clicked()`（约 52-81 行）末尾、`lv_obj_set_style_bg_color(g_dot, ...)` 之前加：

```c
    /* Also fire a toast so the toast-daemon path is exercised on-device. */
    char tmsg[64];
    snprintf(tmsg, sizeof(tmsg), "OK pressed #%d", g_ok_count);
    mc_toast(g_ctx, tmsg, 2000,
             (mc_toast_pos_t)(g_ok_count % 3));   /* cycle bottom/center/top */
```

`mc.h` 已被该文件 include（`#include "mc.h"`），无需新增 include。

- [ ] **Step 2: 交叉构建 + 部署**

Run: `./build.sh T113`（按仓库现有部署流程；产出含 `bin/toast-daemon` 与更新后的 `demo-popup`、`start.sh`）
Expected: 打包成功，`bin/` 下有 `toast-daemon`。

- [ ] **Step 3: 真机行为核对（在设备上跑 start.sh 后逐条确认）**

依次确认：
1. 点 demo-popup 的 OK → 屏幕底/中/顶（随次数轮换）弹出黑底白字卡片，约 2s 后消失。
2. Toast 显示期间，点 Toast 卡片所在区域 → 触摸**穿透**到下层应用（下层有响应；`-v` 日志里 `input: DOWN ... -> sid=` 不是 toast-daemon 的 sid）。
3. 连点两次 OK（间隔 <2s）→ 后一条**立即替换**前一条并重新计时，无残影/叠字。
4. Toast 压在 demo-popup（POPUP 层）之上可见（最上层）。
5. 无 Toast 时，`MC_COMPOSE_TRACE=1` 下其它应用刷新不包含 toast 层（surface 已销毁，零开销）。

记录每条 PASS/FAIL；FAIL 用 `superpowers:systematic-debugging` 处理。

- [ ] **Step 4: 文档**

在 `README.md`（或 `docs/`）加一节「Toast」：API 签名、`ui/toast` 由 `toast-daemon` 渲染、三档位置、默认 2000ms、替换语义、TOAST 角色 z=200 且输入透明。

- [ ] **Step 5: 提交**

```bash
git add examples/demo-popup/main.c README.md
git commit -m "demo+docs: on-device toast trigger via popup OK; document Toast"
```

- [ ] **Step 6: 收尾**

按用户全局约定（阶段性目标达成提醒提交并更新版本记录）：用 `superpowers:finishing-a-development-branch` 决定合并/PR；更新 memory（`t113-awtk-mc-client` 关联一条 toast 实现）。

---

## Self-Review

**Spec 覆盖核对（对照 `docs/superpowers/specs/2026-06-05-toast-design.md`）：**
- 无条件最上层 → Task 2（z=200）✓
- 不占焦点 / 不可触摸 → Task 3（hit-test 跳过 role 4）✓
- 固定时间消失 → Task 5（`g_expire_ms` + `dismiss`）✓
- 可选参数 位置/时长 → Task 1（`mc_toast(text,duration_ms,pos)`）+ Task 5（`band_y_for`/计时）✓
- 替换语义 → Task 5（`show()` 先 `dismiss()`）✓
- 独立守护进程 + 复用 LVGL → Task 5 ✓
- 应用零渲染负担（只发 bus）→ Task 1 ✓
- 默认 2000ms / 底部 / 80% 宽换行 / 三档位置 → Task 1（默认值）+ Task 5（`CARD_MAX_FRAC`/`band_y_for`）✓
- compositor 把 role 4 当 alpha 叠加（不被 role==2 漏掉）→ 已核源码（`compose.c:312` else 分支覆盖），Task 7 Step3 真机核对 ✓
- 不新增 NO_INPUT flag → 绑在 role 上（Task 3）✓

**占位符扫描：** 无 TBD/TODO；每个代码步给了完整代码或精确行号替换。退化方案（LVGL 样式 API）已显式写出，非占位。

**类型一致性：** `mc_toast_pos_t`（0/1/2）↔ 线协议 `pos` 字节 ↔ `band_y_for` 的 `MC_TOAST_POS_*` 一致；`MC_ROLE_TOAST=4` ↔ `default_z_order` case 4 ↔ `input.c` 的 `role==4` ↔ 守护进程 `mc_surface_create_shm(...,MC_ROLE_TOAST,2)` 一致；`mc_toast_wire_encode/decode` 签名在 Task1 定义、Task5 使用一致；`lv_port_mc_init/deinit` 配对一致。

**范围：** 单一特性、单一实现计划，无需拆分。
